# mini-os-simulation
A Java-based Mini Operating System simulation with CLI and GUI support, implementing core file system operations (create, rename, move, list) with modular architecture and interactive command execution.

# How to Run?
javac Main.java core/*.java cli/*.java gui/*.java utils/*.java
java Main